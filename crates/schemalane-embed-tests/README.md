# Embed codegen corpus

This fixture tests sanitized-name collisions and uppercase `.RS`. It is not runnable:
`V9__m.rs` and `V9__m_.rs` deliberately share version 9 to test module-name collision handling.
