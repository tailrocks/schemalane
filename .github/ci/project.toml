# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 2
repository = "tailrocks/schemalane"
profile = "generic"
verified = true
default_branch = "main"
runners = "both"

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-present", "renovate-configuration", "rust-binaries:schemalane-cli:1", "rust-test-targets:schemalane-core:1", "rust-test-targets:schemalane-macros:1", "rust-workspace-members:6"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files."]

[workflow]
github_runner = "ubuntu-24.04"
velnor_labels = ["self-hosted", "velnor-target-mvp"]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml", "release.yml"]
notes = ["Rust verification uses Mr. Boxington 1.11.1 by default on both lanes. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Velnor jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = true
reason = "Publish workspace crates to crates.io in dependency order."
kind = "crates"
packages = ["schemalane-version", "schemalane-macros", "pg_query_fmt", "schemalane-core", "schemalane-cli"]
targets = []

[[unit]]
id = "rust-pg-query-fmt"
label = "Rust crate (pg_query_fmt)"
kind = "rust"
root = "crates/pg_query_fmt"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/pg_query_fmt/**/*.rs", "crates/pg_query_fmt/Cargo.toml", "crates/pg_query_fmt/benches/**", "crates/pg_query_fmt/examples/**", "crates/pg_query_fmt/src/**", "crates/pg_query_fmt/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings"]
github_full_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-cli"
label = "Rust crate (schemalane-cli)"
kind = "rust"
root = "crates/schemalane-cli"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-cli/**/*.rs", "crates/schemalane-cli/Cargo.toml", "crates/schemalane-cli/benches/**", "crates/schemalane-cli/examples/**", "crates/schemalane-cli/src/**", "crates/schemalane-cli/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings"]
github_full_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings"]
depends_on = ["rust-pg-query-fmt", "rust-schemalane-core", "rust-schemalane-version"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-core"
label = "Rust crate (schemalane-core)"
kind = "rust"
root = "crates/schemalane-core"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-core/**/*.rs", "crates/schemalane-core/Cargo.toml", "crates/schemalane-core/benches/**", "crates/schemalane-core/examples/**", "crates/schemalane-core/src/**", "crates/schemalane-core/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings"]
github_full_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings"]
depends_on = ["rust-schemalane-macros", "rust-schemalane-version"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-embed-tests"
label = "Rust crate (schemalane-embed-tests)"
kind = "rust"
root = "crates/schemalane-embed-tests"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-embed-tests/**/*.rs", "crates/schemalane-embed-tests/Cargo.toml", "crates/schemalane-embed-tests/benches/**", "crates/schemalane-embed-tests/examples/**", "crates/schemalane-embed-tests/src/**", "crates/schemalane-embed-tests/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings"]
github_full_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings"]
depends_on = ["rust-schemalane-cli", "rust-schemalane-core"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-macros"
label = "Rust crate (schemalane-macros)"
kind = "rust"
root = "crates/schemalane-macros"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-macros/**/*.rs", "crates/schemalane-macros/Cargo.toml", "crates/schemalane-macros/benches/**", "crates/schemalane-macros/examples/**", "crates/schemalane-macros/src/**", "crates/schemalane-macros/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings"]
github_full_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings"]
depends_on = ["rust-schemalane-version"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-version"
label = "Rust crate (schemalane-version)"
kind = "rust"
root = "crates/schemalane-version"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-version/**/*.rs", "crates/schemalane-version/Cargo.toml", "crates/schemalane-version/benches/**", "crates/schemalane-version/examples/**", "crates/schemalane-version/src/**", "crates/schemalane-version/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
github_pr_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings"]
github_full_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings"]
velnor_pr_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings"]
velnor_full_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings"]
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
