# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/schemalane"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = ["cargo-lockfile", "mise-present", "renovate-configuration", "rust-binaries:schemalane-cli:1", "rust-test-targets:schemalane-core:1", "rust-test-targets:schemalane-macros:1", "rust-workspace-members:6"]
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs.", "Renovate credentials, runner placement, and write permissions cannot be inferred from repository files."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-rust.yml", "maintenance.yml", "nightly.yml"]
notes = ["Rust verification uses Mr. Boxington 1.12.0 on providers [github-hosted]. GitHub-hosted jobs use its GitHub cache backend with bounded snapshot keys (a key names the toolchain/image/linker compatibility class and the hashed source state, so a new source state saves a new snapshot and retention keeps generations bounded). Local-provider jobs use an explicit local-backend setup step against the image/runner-provided local store."]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "rust-pg-query-fmt"
label = "Rust crate (pg_query_fmt)"
kind = "rust"
root = "crates/pg_query_fmt"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/pg_query_fmt/**/*.rs", "crates/pg_query_fmt/Cargo.toml", "crates/pg_query_fmt/benches/**", "crates/pg_query_fmt/examples/**", "crates/pg_query_fmt/src/**", "crates/pg_query_fmt/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'"]
full_commands = ["cd -- 'crates/pg_query_fmt' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/pg_query_fmt' && mbx clippy --locked --profile test --all-targets --all-features --package 'pg_query_fmt' -- -D warnings", "cd -- 'crates/pg_query_fmt' && mbx test --locked --all-features --package 'pg_query_fmt'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/pg_query_fmt' && mbx check --locked --profile test --all-targets --all-features --package 'pg_query_fmt'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-cli"
label = "Rust crate (schemalane-cli)"
kind = "rust"
root = "crates/schemalane-cli"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-cli/**/*.rs", "crates/schemalane-cli/Cargo.toml", "crates/schemalane-cli/benches/**", "crates/schemalane-cli/examples/**", "crates/schemalane-cli/src/**", "crates/schemalane-cli/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'"]
full_commands = ["cd -- 'crates/schemalane-cli' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-cli' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-cli' -- -D warnings", "cd -- 'crates/schemalane-cli' && mbx test --locked --all-features --package 'schemalane-cli'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/schemalane-cli' && mbx check --locked --profile test --all-targets --all-features --package 'schemalane-cli'"]
depends_on = ["rust-pg-query-fmt", "rust-schemalane-core", "rust-schemalane-version"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-core"
label = "Rust crate (schemalane-core)"
kind = "rust"
root = "crates/schemalane-core"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-core/**/*.rs", "crates/schemalane-core/Cargo.toml", "crates/schemalane-core/benches/**", "crates/schemalane-core/examples/**", "crates/schemalane-core/src/**", "crates/schemalane-core/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'"]
full_commands = ["cd -- 'crates/schemalane-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-core' -- -D warnings", "cd -- 'crates/schemalane-core' && mbx test --locked --all-features --package 'schemalane-core'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/schemalane-core' && mbx check --locked --profile test --all-targets --all-features --package 'schemalane-core'"]
depends_on = ["rust-schemalane-macros", "rust-schemalane-version"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-embed-tests"
label = "Rust crate (schemalane-embed-tests)"
kind = "rust"
root = "crates/schemalane-embed-tests"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-embed-tests/**/*.rs", "crates/schemalane-embed-tests/Cargo.toml", "crates/schemalane-embed-tests/benches/**", "crates/schemalane-embed-tests/examples/**", "crates/schemalane-embed-tests/src/**", "crates/schemalane-embed-tests/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'"]
full_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-embed-tests' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests' -- -D warnings", "cd -- 'crates/schemalane-embed-tests' && mbx test --locked --all-features --package 'schemalane-embed-tests'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/schemalane-embed-tests' && mbx check --locked --profile test --all-targets --all-features --package 'schemalane-embed-tests'"]
depends_on = ["rust-schemalane-cli", "rust-schemalane-core"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-macros"
label = "Rust crate (schemalane-macros)"
kind = "rust"
root = "crates/schemalane-macros"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-macros/**/*.rs", "crates/schemalane-macros/Cargo.toml", "crates/schemalane-macros/benches/**", "crates/schemalane-macros/examples/**", "crates/schemalane-macros/src/**", "crates/schemalane-macros/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'"]
full_commands = ["cd -- 'crates/schemalane-macros' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-macros' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-macros' -- -D warnings", "cd -- 'crates/schemalane-macros' && mbx test --locked --all-features --package 'schemalane-macros'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/schemalane-macros' && mbx check --locked --profile test --all-targets --all-features --package 'schemalane-macros'"]
depends_on = ["rust-schemalane-version"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]

[[unit]]
id = "rust-schemalane-version"
label = "Rust crate (schemalane-version)"
kind = "rust"
root = "crates/schemalane-version"
watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/schemalane-version/**/*.rs", "crates/schemalane-version/Cargo.toml", "crates/schemalane-version/benches/**", "crates/schemalane-version/examples/**", "crates/schemalane-version/src/**", "crates/schemalane-version/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
pr_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'"]
full_commands = ["cd -- 'crates/schemalane-version' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/schemalane-version' && mbx clippy --locked --profile test --all-targets --all-features --package 'schemalane-version' -- -D warnings", "cd -- 'crates/schemalane-version' && mbx test --locked --all-features --package 'schemalane-version'"]
phases = ["fmt", "clippy", "test"]
check_commands = ["cd -- 'crates/schemalane-version' && mbx check --locked --profile test --all-targets --all-features --package 'schemalane-version'"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = true
nested_privileged_docker = false
buildx_compose = false
testcontainers = true
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
[unit.cache]
key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
paths = ["~/.cargo/registry", "~/.cargo/git"]
