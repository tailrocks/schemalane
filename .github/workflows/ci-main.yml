# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / Main
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      runner:
        description: Execution backend
        required: false
        default: both
        type: choice
        options:
          - velnor
          - github
          - both
      scope:
        description: Verification scope
        required: true
        default: full
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      full_units: ${{ steps.plan.outputs.full_units }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        id: runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
        with:
          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_LANES: ${{ github.event.inputs.runner || 'both' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          policy_closure="$("$stage/velnor-workflow-policy" --closure)"
          [[ "$policy_closure" == "${{ steps.runtime.outputs.closure }}" ]] || { echo "::error::policy runtime reports closure $policy_closure, expected ${{ steps.runtime.outputs.closure }}" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg closure "${{ steps.runtime.outputs.closure }}" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_closure "$policy_closure" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, closure: $closure, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_closure: $policy_closure, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  velnor-lane-admission:
    name: "Control / Velnor admission"
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Velnor lane omitted for fork
        run: |
          echo '::notice::Velnor lane omitted for fork pull request; validating GitHub lane only.'
  policy:
    name: Policy
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    # Trust invariant: this job runs the base branch's Stage-0 validator
    # product against the audited tree under pull_request_target. It holds
    # `contents: read` only, references no secrets, persists no credentials,
    # and never compiles. When the audited tree differs from the declared
    # pin's render, it additionally EXECUTES the PR run's prebuilt
    # candidate generator — PR-built code, same-repository runs only, bound
    # to the audited tree by manifest closure plus binary digest before
    # execution — with no secret references, no persisted credentials, the
    # read-only github.token confined to the Acquire/Ruleset API steps,
    # and both candidate exec points tokenless.
    permissions:
      contents: read
    steps:
      - name: Checkout repository history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: policy-checkout
          fetch-depth: 0
          persist-credentials: false
      - name: Check out audited head
        working-directory: policy-checkout
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
        run: |
          set -euo pipefail
          if ! git cat-file -e "$HEAD_SHA^{commit}" 2>/dev/null; then
            git fetch --no-tags "$GITHUB_SERVER_URL/$HEAD_REPOSITORY" "$HEAD_SHA"
          fi
          git checkout --quiet --detach "$HEAD_SHA"
      - name: Set up Velnor workflow runtime
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
        with:
          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
          checkout-path: ${{ github.workspace }}/policy-checkout
      - name: Read declared generator pin
        id: pin
        working-directory: policy-checkout
        run: |
          set -euo pipefail
          pin="$(sed -n -E 's/^[[:space:]]*revision[[:space:]]*=[[:space:]]*"([0-9a-f]{40})".*/\1/p' .github-gen/velnor-workflow.toml | head -n 1)"
          test "$pin" != '' || pin="$(sed -n -E 's/^.*VELNOR_WORKFLOW_POLICY_REVISION:[[:space:]]*([0-9a-f]{40}).*/\1/p' .github/workflows/ci-policy.yml | head -n 1)"
          test "$pin" != '' || { echo "::error::audited tree declares no generator pin" >&2; exit 1; }
          echo "value=$pin" >> "$GITHUB_OUTPUT"
      - name: Set up declared generator product
        id: renderer
        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
        with:
          rev: ${{ steps.pin.outputs.value }}
          checkout-path: ${{ github.workspace }}/policy-checkout
      - name: Resolve declared generator product
        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
        run: |
          set -euo pipefail
          binary="$HOME/.cache/velnor/workflow-runtime/${{ steps.renderer.outputs.closure }}/bin/velnor-workflow"
          test -x "$binary"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$binary" >> "$GITHUB_ENV"
      - name: Resolve required status checks
        env:
          GH_TOKEN: ${{ github.token }}
          DEFAULT_BRANCH: main
          DECLARED_RULESET_CONTEXTS: DCO,Policy,ci-required
        run: |
          set -euo pipefail
          stderr="$(mktemp)"
          trap 'rm -f "$stderr"' EXIT
          if contexts="$(gh api "repos/$GITHUB_REPOSITORY/rulesets?includes_parents=true" 2>"$stderr" \
            | jq -r '.[] | select(.target == "branch" and .enforcement == "active") | .id' \
            | while read -r id; do gh api "repos/$GITHUB_REPOSITORY/rulesets/$id"; done \
            | jq -r --arg branch "refs/heads/$DEFAULT_BRANCH" 'select(.conditions.ref_name.include | any(. == "~DEFAULT_BRANCH" or . == "~ALL" or . == $branch)) | .rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context' \
            | sort -u | paste -sd, -)"; then
            :
          elif grep -qE '(HTTP 403|Upgrade to GitHub Team)' "$stderr"; then
            echo "::warning::rulesets API returned 403; falling back to declared contexts [$DECLARED_RULESET_CONTEXTS]"
            contexts="$DECLARED_RULESET_CONTEXTS"
          else
            cat "$stderr" >&2
            exit 1
          fi
          echo "RULESET_CONTEXTS=$contexts" >> "$GITHUB_ENV"
      - name: Enforce workflow policy
        env:
          WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
          VELNOR_WORKFLOW_POLICY_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
        run: |
          set -euo pipefail
          velnor-workflow policy \
            --workflow-root "$WORKFLOW_ROOT" \
            --head-sha "$HEAD_SHA" \
            --base-sha "$BASE_SHA" \
            --candidate-manifest "${VELNOR_WORKFLOW_CANDIDATE_MANIFEST:-}" \
            --ruleset-contexts "$RULESET_CONTEXTS"

      - name: Set up actionlint
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: actionlint@1.7.12
          cache: false
      - name: Lint caller workflows
        working-directory: policy-checkout
        env:
          MISE_NO_CONFIG: "1"
        run: mise exec actionlint@1.7.12 -- actionlint
  prepare-cargo:
    name: "Control / Prepare Cargo"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') || contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-cli,') || contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-core,') || contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-embed-tests,') || contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-macros,') || contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-version,')) && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: control
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-pg-query-fmt:
    name: "Rust · pg_query_fmt"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: f6833e7ff7c0
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/pg_query_fmt"
      cargo_net_offline: true
      unit_admission: github
  velnor-rust-pg-query-fmt:
    name: "Rust · pg_query_fmt"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_admission: velnor
  github-rust-schemalane-cli:
    name: "Rust · schemalane-cli"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-cli,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-cli
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "87af2472eb01"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        crates/schemalane-cli/**/*.rs
        crates/schemalane-cli/benches/**
        crates/schemalane-cli/examples/**
        crates/schemalane-cli/src/**
        crates/schemalane-cli/tests/**
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-cli"
      cargo_net_offline: true
      unit_dependencies: "rust-pg-query-fmt,rust-schemalane-core,rust-schemalane-version"
      unit_admission: github
  velnor-rust-schemalane-cli:
    name: "Rust · schemalane-cli"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-cli,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-cli
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_dependencies: "rust-pg-query-fmt,rust-schemalane-core,rust-schemalane-version"
      unit_admission: velnor
  github-rust-schemalane-core:
    name: "Rust · schemalane-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-core,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-core
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "45db5d4ca3db"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-core"
      cargo_net_offline: true
      unit_dependencies: "rust-schemalane-macros,rust-schemalane-version"
      unit_admission: github
  velnor-rust-schemalane-core:
    name: "Rust · schemalane-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-core,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-core
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_dependencies: "rust-schemalane-macros,rust-schemalane-version"
      unit_admission: velnor
  github-rust-schemalane-embed-tests:
    name: "Rust · schemalane-embed-tests"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-embed-tests,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-embed-tests
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: f07746e77dba
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        crates/schemalane-cli/**/*.rs
        crates/schemalane-cli/benches/**
        crates/schemalane-cli/examples/**
        crates/schemalane-cli/src/**
        crates/schemalane-cli/tests/**
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-embed-tests/**/*.rs
        crates/schemalane-embed-tests/benches/**
        crates/schemalane-embed-tests/examples/**
        crates/schemalane-embed-tests/src/**
        crates/schemalane-embed-tests/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-embed-tests"
      cargo_net_offline: true
      unit_dependencies: "rust-schemalane-cli,rust-schemalane-core"
      unit_admission: github
  velnor-rust-schemalane-embed-tests:
    name: "Rust · schemalane-embed-tests"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-embed-tests,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-embed-tests
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_dependencies: "rust-schemalane-cli,rust-schemalane-core"
      unit_admission: velnor
  github-rust-schemalane-macros:
    name: "Rust · schemalane-macros"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-macros,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-macros
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "3d0463253fe2"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-macros"
      cargo_net_offline: true
      unit_dependencies: rust-schemalane-version
      unit_admission: github
  velnor-rust-schemalane-macros:
    name: "Rust · schemalane-macros"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-macros,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-macros
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_dependencies: rust-schemalane-version
      unit_admission: velnor
  github-rust-schemalane-version:
    name: "Rust · schemalane-version"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-version,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-version
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      mbx_compat: "1359c090b14d"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-version"
      cargo_net_offline: true
      unit_admission: github
  velnor-rust-schemalane-version:
    name: "Rust · schemalane-version"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-version,') && ((github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-version
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_enabled: true
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
      unit_admission: velnor
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, velnor-lane-admission, policy, prepare-cargo, github-rust-pg-query-fmt, velnor-rust-pg-query-fmt, github-rust-schemalane-cli, velnor-rust-schemalane-cli, github-rust-schemalane-core, velnor-rust-schemalane-core, github-rust-schemalane-embed-tests, velnor-rust-schemalane-embed-tests, github-rust-schemalane-macros, velnor-rust-schemalane-macros, github-rust-schemalane-version, velnor-rust-schemalane-version]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          LANE_ADMITTED_GITHUB: ${{ github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == '')) }}
          LANE_ADMITTED_VELNOR: ${{ (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == '')) }}
        shell: bash
        run: |
          set -euo pipefail
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job policy)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite policy did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job velnor-lane-admission)"
          case "$result" in
            success|skipped) ;;
            *) echo "required CI prerequisite velnor-lane-admission did not pass: $result" >&2; exit 1 ;;
          esac
          selected=",$SELECTED_UNITS,"
          if [[ "$selected" == *",rust-pg-query-fmt,"* || "$selected" == *",rust-schemalane-cli,"* || "$selected" == *",rust-schemalane-core,"* || "$selected" == *",rust-schemalane-embed-tests,"* || "$selected" == *",rust-schemalane-macros,"* || "$selected" == *",rust-schemalane-version,"* ]]; then
            result="$(result_for_job prepare-cargo)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI prerequisite prepare-cargo did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI prerequisite prepare-cargo ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job prepare-cargo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI prerequisite prepare-cargo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-pg-query-fmt,"* ]]; then
            result="$(result_for_job github-rust-pg-query-fmt)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-pg-query-fmt did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-pg-query-fmt ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-pg-query-fmt)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-pg-query-fmt failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-pg-query-fmt,"* ]]; then
            result="$(result_for_job velnor-rust-pg-query-fmt)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-pg-query-fmt did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-pg-query-fmt ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-pg-query-fmt)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-pg-query-fmt failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-cli,"* ]]; then
            result="$(result_for_job github-rust-schemalane-cli)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-schemalane-cli did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-schemalane-cli ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-schemalane-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-cli,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-cli)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-cli did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-cli ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-core,"* ]]; then
            result="$(result_for_job github-rust-schemalane-core)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-schemalane-core did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-schemalane-core ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-schemalane-core)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-core failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-core,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-core)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-core did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-core ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-core)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-core failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-embed-tests,"* ]]; then
            result="$(result_for_job github-rust-schemalane-embed-tests)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-schemalane-embed-tests did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-schemalane-embed-tests ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-schemalane-embed-tests)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-embed-tests failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-embed-tests,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-embed-tests)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-embed-tests did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-embed-tests ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-embed-tests)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-embed-tests failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-macros,"* ]]; then
            result="$(result_for_job github-rust-schemalane-macros)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-schemalane-macros did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-schemalane-macros ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-schemalane-macros)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-macros failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-macros,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-macros)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-macros did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-macros ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-macros)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-macros failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-version,"* ]]; then
            result="$(result_for_job github-rust-schemalane-version)"
            if [[ "$LANE_ADMITTED_GITHUB" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-schemalane-version did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job github-rust-schemalane-version ran outside its lane admission (LANE_ADMITTED_GITHUB=$LANE_ADMITTED_GITHUB): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-schemalane-version)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-version failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-version,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-version)"
            if [[ "$LANE_ADMITTED_VELNOR" == true ]]; then
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-version did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-version ran outside its lane admission (LANE_ADMITTED_VELNOR=$LANE_ADMITTED_VELNOR): $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-version)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-version failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
