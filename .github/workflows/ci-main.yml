# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / Main
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      runner:
        description: Execution backend
        required: false
        default: both
        type: choice
        options:
          - velnor
          - github
          - both
      scope:
        description: Verification scope
        required: true
        default: full
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: ci-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      full_units: ${{ steps.plan.outputs.full_units }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@541d8926737213542156ee32e420010817f8a323
        with:
          rev: 541d8926737213542156ee32e420010817f8a323
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=541d8926737213542156ee32e420010817f8a323" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_LANES: ${{ github.event.inputs.runner || 'both' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: 541d8926737213542156ee32e420010817f8a323
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          policy_src="${VELNOR_WORKFLOW_PINNED_BINARY:-$src}"
          install -m 0755 "$policy_src" "$stage/velnor-workflow-policy"
          policy_revision="$("$stage/velnor-workflow-policy" --revision)"
          [[ "$policy_revision" == "$EXPECTED_REVISION" ]] || { echo "::error::policy runtime reports revision $policy_revision, expected $EXPECTED_REVISION" >&2; exit 1; }
          policy_digest="$(sha256sum "$stage/velnor-workflow-policy" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" --arg policy_revision "$policy_revision" --arg policy_binary_sha256 "$policy_digest" '{repository: $repository, revision: $revision, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256, policy_revision: $policy_revision, policy_binary_sha256: $policy_binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-541d8926737213542156ee32e420010817f8a323-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  velnor-lane-admission:
    name: "Control / Velnor admission"
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Velnor lane omitted for fork
        run: |
          echo '::notice::Velnor lane omitted for fork pull request; validating GitHub lane only.'
  policy:
    name: Policy
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    # Trust invariant: this job runs the base branch's validator against the
    # audited tree under pull_request_target. It holds `contents: read` only,
    # references no secrets, and its checkout persists no credentials, so
    # building and running the tree's declared generator here is no more
    # privileged than the pull_request lanes that already build the tree.
    permissions:
      contents: read
    steps:
      - name: Checkout repository history
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          path: policy-checkout
          fetch-depth: 0
          persist-credentials: false
      - name: Check out audited head
        working-directory: policy-checkout
        env:
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          HEAD_REPOSITORY: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
        run: |
          set -euo pipefail
          if ! git cat-file -e "$HEAD_SHA^{commit}" 2>/dev/null; then
            git fetch --no-tags "$GITHUB_SERVER_URL/$HEAD_REPOSITORY" "$HEAD_SHA"
          fi
          git checkout --quiet --detach "$HEAD_SHA"
      - name: Bound the Mr. Boxington store
        shell: bash
        run: echo "MBX_GC_MAX_SIZE=12GiB" >> "$GITHUB_ENV"
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          version: 1.11.1
          cache-key: velnor-policy-mbx-1.11.1-${{ runner.os }}-${{ runner.arch }}-541d8926737213542156ee32e420010817f8a323
          restore-keys: |
            velnor-policy-mbx-1.11.1-${{ runner.os }}-${{ runner.arch }}-
      - name: Install pinned Velnor workflow runtime
        env:
          CARGO_HOME: ${{ runner.temp }}/velnor-workflow-cargo-home
          CARGO_TARGET_DIR: ${{ runner.temp }}/velnor-workflow-cargo-target
          VELNOR_WORKFLOW_INSTALL_DIR: ${{ runner.temp }}/velnor-workflow-install
          VELNOR_WORKFLOW_ROOT: ${{ runner.temp }}/velnor-workflow
        run: |
          set -euo pipefail
          install -d -m 700 \
            "$CARGO_HOME" \
            "$CARGO_TARGET_DIR" \
            "$VELNOR_WORKFLOW_INSTALL_DIR"
          cd "$VELNOR_WORKFLOW_INSTALL_DIR"
          env -u RUSTC_WRAPPER -u SCCACHE_GHA_ENABLED -u CARGO_INCREMENTAL -u RUSTFLAGS -u CARGO_ENCODED_RUSTFLAGS \
            cargo install \
            --locked \
            --git https://github.com/tailrocks/velnor \
            --rev 541d8926737213542156ee32e420010817f8a323 \
            --root "$VELNOR_WORKFLOW_ROOT" \
            velnor-workflow \
            --bin velnor-workflow
          echo "$VELNOR_WORKFLOW_ROOT/bin" >> "$GITHUB_PATH"
      - name: Resolve required status checks
        env:
          GH_TOKEN: ${{ github.token }}
          DEFAULT_BRANCH: main
        run: |
          set -euo pipefail
          contexts="$(gh api "repos/$GITHUB_REPOSITORY/rulesets?includes_parents=true" \
            | jq -r '.[] | select(.target == "branch" and .enforcement == "active") | .id' \
            | while read -r id; do gh api "repos/$GITHUB_REPOSITORY/rulesets/$id"; done \
            | jq -r --arg branch "refs/heads/$DEFAULT_BRANCH" 'select(.conditions.ref_name.include | any(. == "~DEFAULT_BRANCH" or . == "~ALL" or . == $branch)) | .rules[] | select(.type == "required_status_checks") | .parameters.required_status_checks[].context' \
            | sort -u | paste -sd, -)"
          echo "RULESET_CONTEXTS=$contexts" >> "$GITHUB_ENV"
      - name: Enforce workflow policy
        env:
          WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
          HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
          VELNOR_WORKFLOW_POLICY_REVISION: 541d8926737213542156ee32e420010817f8a323
        run: |
          set -euo pipefail
          velnor-workflow policy \
            --workflow-root "$WORKFLOW_ROOT" \
            --head-sha "$HEAD_SHA" \
            --base-sha "$BASE_SHA" \
            --ruleset-contexts "$RULESET_CONTEXTS"
      - name: Set up actionlint
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: actionlint@1.7.12
          cache: false
      - name: Lint caller workflows
        working-directory: policy-checkout
        run: mise exec actionlint@1.7.12 -- actionlint
  prepare-cargo:
    name: "Control / Prepare Cargo"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: control
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-pg-query-fmt:
    name: "Rust · pg_query_fmt"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: f661399baa15
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/pg_query_fmt"
      cargo_net_offline: true
  velnor-rust-pg-query-fmt:
    name: "Rust · pg_query_fmt"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-pg-query-fmt,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-pg-query-fmt
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-schemalane-cli:
    name: "Rust · schemalane-cli"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-cli,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-cli
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: d2a29f467f96
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        crates/schemalane-cli/**/*.rs
        crates/schemalane-cli/benches/**
        crates/schemalane-cli/examples/**
        crates/schemalane-cli/src/**
        crates/schemalane-cli/tests/**
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-cli"
      cargo_net_offline: true
  velnor-rust-schemalane-cli:
    name: "Rust · schemalane-cli"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-cli,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-cli
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-schemalane-core:
    name: "Rust · schemalane-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-core,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-core
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: b6c3e280ea7a
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-core"
      cargo_net_offline: true
  velnor-rust-schemalane-core:
    name: "Rust · schemalane-core"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-core,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-core
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-schemalane-embed-tests:
    name: "Rust · schemalane-embed-tests"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-embed-tests,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-embed-tests
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: "19b1d40b3733"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/pg_query_fmt/**/*.rs
        crates/pg_query_fmt/benches/**
        crates/pg_query_fmt/examples/**
        crates/pg_query_fmt/src/**
        crates/pg_query_fmt/tests/**
        crates/schemalane-cli/**/*.rs
        crates/schemalane-cli/benches/**
        crates/schemalane-cli/examples/**
        crates/schemalane-cli/src/**
        crates/schemalane-cli/tests/**
        crates/schemalane-core/**/*.rs
        crates/schemalane-core/benches/**
        crates/schemalane-core/examples/**
        crates/schemalane-core/src/**
        crates/schemalane-core/tests/**
        crates/schemalane-embed-tests/**/*.rs
        crates/schemalane-embed-tests/benches/**
        crates/schemalane-embed-tests/examples/**
        crates/schemalane-embed-tests/src/**
        crates/schemalane-embed-tests/tests/**
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-embed-tests"
      cargo_net_offline: true
  velnor-rust-schemalane-embed-tests:
    name: "Rust · schemalane-embed-tests"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-embed-tests,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-embed-tests
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-schemalane-macros:
    name: "Rust · schemalane-macros"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-macros,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-macros
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: "7fedaa2bd6c4"
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-macros/**/*.rs
        crates/schemalane-macros/benches/**
        crates/schemalane-macros/examples/**
        crates/schemalane-macros/src/**
        crates/schemalane-macros/tests/**
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-macros"
      cargo_net_offline: true
  velnor-rust-schemalane-macros:
    name: "Rust · schemalane-macros"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-macros,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-macros
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  github-rust-schemalane-version:
    name: "Rust · schemalane-version"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-version,') }}
    needs: [plan, policy]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-version
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      mbx_compat: e2cb0e2feb8f
      mbx_dependency_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain
        rust-toolchain.toml
      mbx_freshness_files: |
        crates/schemalane-version/**/*.rs
        crates/schemalane-version/benches/**
        crates/schemalane-version/examples/**
        crates/schemalane-version/src/**
        crates/schemalane-version/tests/**
        mise.lock
      cache_paths: |
        ~/.cargo/registry
        ~/.cargo/git
      cache_key_files: |
        .cargo/**
        Cargo.lock
        rust-toolchain.toml
        rust-toolchain
      cargo_root: "crates/schemalane-version"
      cargo_net_offline: true
  velnor-rust-schemalane-version:
    name: "Rust · schemalane-version"
    if: ${{ always() && needs.plan.result == 'success' && needs.policy.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-schemalane-version,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, policy, prepare-cargo]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-schemalane-version
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
      cargo_net_offline: true
      host_warm_layers: "rustup,mold,mbx,cargo"
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, velnor-lane-admission, policy, prepare-cargo, github-rust-pg-query-fmt, velnor-rust-pg-query-fmt, github-rust-schemalane-cli, velnor-rust-schemalane-cli, github-rust-schemalane-core, velnor-rust-schemalane-core, github-rust-schemalane-embed-tests, velnor-rust-schemalane-embed-tests, github-rust-schemalane-macros, velnor-rust-schemalane-macros, github-rust-schemalane-version, velnor-rust-schemalane-version]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          FORK_PR: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job policy)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite policy did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job velnor-lane-admission)"
          case "$result" in
            success|skipped) ;;
            *) echo "required CI prerequisite velnor-lane-admission did not pass: $result" >&2; exit 1 ;;
          esac
          selected=",$SELECTED_UNITS,"
          if [[ "$selected" == *",rust-pg-query-fmt,"* ]]; then
            result="$(result_for_job prepare-cargo)"
            if [[ "$result" != success ]]; then
              echo "selected CI prerequisite prepare-cargo did not pass: $result" >&2
              exit 1
            fi
          else
            result="$(result_for_job prepare-cargo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI prerequisite prepare-cargo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-pg-query-fmt,"* ]]; then
            result="$(result_for_job github-rust-pg-query-fmt)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-pg-query-fmt did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-pg-query-fmt)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-pg-query-fmt failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-pg-query-fmt,"* ]]; then
            result="$(result_for_job velnor-rust-pg-query-fmt)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-pg-query-fmt did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-pg-query-fmt did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-pg-query-fmt)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-pg-query-fmt failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-cli,"* ]]; then
            result="$(result_for_job github-rust-schemalane-cli)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-schemalane-cli did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-schemalane-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-cli,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-cli)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-cli did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-cli did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-cli)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-cli failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-core,"* ]]; then
            result="$(result_for_job github-rust-schemalane-core)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-schemalane-core did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-schemalane-core)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-core failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-core,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-core)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-core did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-core did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-core)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-core failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-embed-tests,"* ]]; then
            result="$(result_for_job github-rust-schemalane-embed-tests)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-schemalane-embed-tests did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-schemalane-embed-tests)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-embed-tests failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-embed-tests,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-embed-tests)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-embed-tests did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-embed-tests did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-embed-tests)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-embed-tests failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-macros,"* ]]; then
            result="$(result_for_job github-rust-schemalane-macros)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-schemalane-macros did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-schemalane-macros)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-macros failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-macros,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-macros)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-macros did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-macros did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-macros)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-macros failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-version,"* ]]; then
            result="$(result_for_job github-rust-schemalane-version)"
            case "$result" in
              success) ;;
              *) echo "selected CI job github-rust-schemalane-version did not pass: $result" >&2; exit 1 ;;
            esac
          else
            result="$(result_for_job github-rust-schemalane-version)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-schemalane-version failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-schemalane-version,"* ]]; then
            result="$(result_for_job velnor-rust-schemalane-version)"
            if [[ "$FORK_PR" == true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-schemalane-version did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-schemalane-version did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-schemalane-version)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-schemalane-version failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
